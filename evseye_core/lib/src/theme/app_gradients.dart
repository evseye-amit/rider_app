import 'package:flutter/material.dart';

import 'app_colors.dart';

abstract final class AppGradients {
  static const Color band = AppColors.ink;

  static const Color iris = AppColors.primary;

  static const Color heroWash = AppColors.primaryWash;

  static const Color bloom = AppColors.primaryWash;

  const AppGradients._();
}
